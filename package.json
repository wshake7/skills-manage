{
  "name": "@skills-manage/root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "tsc -b packages/schemas packages/core packages/providers packages/cli packages/local-ui packages/cloud-ui",
    "check": "tsc -b --pretty false packages/schemas packages/core packages/providers packages/cli packages/local-ui packages/cloud-ui",
    "clean": "pnpm -r exec node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\""
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
