# 2026-04-29

## 今日记录

- 根据 `requirement.md` 梳理并确定 v1 技术栈，已写入 `MEMORY.md`。
- 技术栈核心决策：Node.js + TypeScript monorepo；暂定 pnpm workspace、React + Vite、Fastify；按 core、cli、local-ui、cloud-ui、providers、schemas、templates 拆分。
- 开始实施 v1 计划：搭建 pnpm + TypeScript monorepo，新增 schemas/core/providers/cli/local-ui/cloud-ui 包边界，加入初始化模板和 GitHub Actions 模板。
- 验证结果：`pnpm check` 通过；临时目录中 `init-project` 和 `doctor --layer project` 冒烟通过。
- 新增中英双语 `README.md`，说明项目愿景、三层模型、当前技术栈、仓库结构、配置文件、CLI、开发命令与当前进度。
- 将 README 拆分为中文 `README.md` 与英文 `README.en.md`，并补充云端级、系统级、项目级的安装与使用流程。
- 优化 CLI 本地全局安装：新增 `pnpm global:install` / `pnpm global:uninstall`，安装后可直接运行 `skills-manage --help`。
- 缩短 CLI 命令：新增 `sm` 全局命令；`sm ui` 默认从 `~/.skills-manage` 启动系统级 UI，可在任意目录运行。
- 修复 `sm ui` 只输出占位文本的问题：新增最小本地 HTTP UI，支持首页、`/health` 和 `/api/status`。
- 使用 `ui-ux-pro-max` 重新设计本地 UI：首页从朴素占位页升级为数据密集型管理控制台，包含导航、指标卡、source 表格、层级解析、工作区详情和 endpoint 区域。
- 根据界面反馈移除首页中的请求端点展示，改为面向用户的 Runtime Guardrails 区域；底层 `/health` 和 `/api/status` 保留为内部接口。
- 根据反馈调整产品入口：用户想要云端直接 fork 后拥有能力，而不是先走本地安装和 `init-cloud`。已把仓库改成 fork-first 云端模板：根目录新增 `skills-cloud.config.json`，激活 `.github/workflows/*`，保留 `public/data/` 与 `.agents/skills/` 目录约定，并同步更新 README/README.en/requirement。
- 将云端默认 AI provider 切换为 DeepSeek：根目录和 cloud 模板默认 `provider: "deepseek"`，`update-skills` workflow 通过 GitHub Actions secret `DEEPSEEK_API_KEY` 注入密钥，并支持 `DEEPSEEK_MODEL`、`DEEPSEEK_BASE_URL` repository variables。
- DeepSeek provider 从占位生成升级为调用 Chat Completions API；同时修正云端 workflows 在 `pnpm --filter skills-manage exec` 下的工作目录，统一通过 `--dir ../..` 读取仓库根配置。
- 补充每个 GitHub Actions workflow 的用途说明：README/README.en 新增使用场景、执行命令、配置需求和主要产物表格；requirement 中记录后续 workflow 文档约束。
- 修复 GitHub Actions 中 `pnpm/action-setup@v4` 与根 `packageManager: pnpm@9.15.0` 的版本冲突：删除 workflow 和模板中的显式 `version: 9`，统一由 `package.json` 管理 pnpm 版本。
- 修复 GitHub Actions 中 `pnpm --filter skills-manage exec skills-manage ...` 找不到当前包 bin 的问题：workflow 和模板改为先 `pnpm build`，再直接执行 `node packages/cli/dist/index.js ... --dir .`。
- 补齐 `ai-update` 的可见效果：现在会解析 enabled sources，调用 provider 生成 `.agents/skills/<source-id>/SKILL.md`，写入 `skill.manifest.json`，并让 `update-skills.yml` 在 `.agents/skills` 有变化时自动提交推送。
- 根据反馈调整云端更新策略：`update-skills.yml` 不再直接推送到主分支，而是创建更新分支并打开 PR；覆盖已有 skill 前会归档旧版本到 `.agents/skill-archives/<skill>/recent/`，最近 10 个以外移动到 `older/`。
- 完善 `release-skills.yml` 的 GitHub Pages 部署：workflow 现在配置 Pages、上传 `dist/cloud-ui` artifact 并部署；`publish-cloud-ui` 会生成只读 `index.html` 和快照数据。
